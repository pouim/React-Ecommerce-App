import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  root: {
    display: "flex",
    alignItems: 'center',
    justifyContent: 'center',
    position: 'relative',
    bottom: '0',
    left: '0',
    maxHeight: '2rem',
    padding: "2rem",
    borderTop: "0.1rem solid rgba(0, 0, 0, 0.09996)",
  },
}));
